import styled from "styled-components";

export const Liststyles = styled.div`
  display:flex;
  flex-direction: column;
  margin:20px;
  & > p{
    color:white;
    font-size:15px;
    font-weight:800;
  }
`